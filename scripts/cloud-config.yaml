#cloud-config

package_update: true
package_upgrade: true
packages:
  - curl
  - gnupg2
  - lsb-release
  - apt-transport-https
  - ca-certificates
  - openjdk-8-jdk

runcmd:
  # Use Java 8 (required by CycleCloud)
  - update-alternatives --set java /usr/lib/jvm/java-8-openjdk-amd64/jre/bin/java

  # Add Microsoft signing key (shared by Azure CLI and CycleCloud repos)
  - install -d -m 0755 /etc/apt/keyrings
  - curl -fsSL https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor -o /etc/apt/keyrings/microsoft.gpg
  - chmod go+r /etc/apt/keyrings/microsoft.gpg

  # Configure Azure CLI apt repository
  - |
    AZ_DIST=$(lsb_release -cs)
    cat <<EOF > /etc/apt/sources.list.d/azure-cli.sources
    Types: deb
    URIs: https://packages.microsoft.com/repos/azure-cli/
    Suites: ${AZ_DIST}
    Components: main
    Architectures: $(dpkg --print-architecture)
    Signed-by: /etc/apt/keyrings/microsoft.gpg
    EOF

  # Configure CycleCloud apt repository
  - echo "deb [signed-by=/etc/apt/keyrings/microsoft.gpg] https://packages.microsoft.com/repos/cyclecloud stable main" > /etc/apt/sources.list.d/cyclecloud.list

  # Refresh apt and install Azure CLI + CycleCloud
  - apt-get -qq update
  - DEBIAN_FRONTEND=noninteractive apt-get -y install azure-cli cyclecloud8